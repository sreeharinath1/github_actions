name: e.manualtriggger
on:
  workflow_dispatch:
    inputs:
      environments:
        description: "Select env"
        type: choice
        required: true
        options: [prod, dev, stage]
      skip_tests:
        description: "Select TEST"
        type: boolean
        required: false
        default: true
      version:
        description: "Enter application Version"
        type: string
        required: true
        default: "sree"


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Ran a Script
        run: echo "iam running"
name: w.githupermissions
on:
  workflow_dispatch:
permissions:
  contents: none
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: content permission
        uses: actions/checkout@v4
      - name: editing a file
        run: |
          echo "ouput" > output.txt
          git push
      - name: list repo file
        run: ls -la

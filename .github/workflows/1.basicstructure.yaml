name: a.basic structure flow
on:
  push:
    branches: [ main ]
jobs:
  build: # job name
    runs-on: ubuntu-latest # runner types
    steps:
      - uses: actions/checkout@v4
      - name: Run a Script
        run: echo "Hi GitHub Actions World"
